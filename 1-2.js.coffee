app.createChallenge '1-2', ->

  # Model Instance
  #
  # Instructions:
  #
  # Now that we have our Appointment model class, let's create our
  # first instance and assign it to the `appointment` variable.
  #
  # initial code:
  `var appointment;`
  
  # Answer:
  `var appointment = new Appointment()`
