app.createChallenge('6-1', function(){
  // App Object

  // Instructions:
  /*
    Time to get organized. Dr. Goodparts really hates global scope
    and has ordered that we only have at most 1 object polluting his precious
    global scope. 

    Create an object that contains an empty `Collections`, `Models`, and `Views` object.
    Name the top-level object `AppointmentApp`.
  */

  // Setup:

  // Initial Code:

  // Answer:
  var AppointmentApp = {
    Collections: {},
    Models: {},
    Views: {}
  }
  
});