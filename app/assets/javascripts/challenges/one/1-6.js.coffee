app.createChallenge '1-6', ->

  # Set the title
  #
  # Instructions:
  #
  # Almost there, but before we can display anything, we have
  # to have something to display!  Remember back in challenge 1-2, when
  # we created our `Appointment` instance?  Well, we need to give it
  # some data before it's displayed.  
  #
  # Set the title of the `Appointment` instance you created to 
  # something you'd like Dr. Goodparts to take a look at.
  #
  #
  # Answer:
  appointment.set({title: 'My knee hurts'});

  # Alternate Answer:
  appointment.set('title', 'My knee hurts');





