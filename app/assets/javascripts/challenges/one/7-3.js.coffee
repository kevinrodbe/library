app.createChallenge '7-3', ->

  # Start the History
  #
  # Instructions:
  # 
  # Before we can start to use our Router we have
  # to make sure Backbone starts watching for history changes.
  #
  # Go ahead and start history now, and make sure pushState is on.
  #
  # Initial Code

  # Answer:
  `Backbone.history.start({pushState: true});`

  # need this or coffeescript will produce a js syntax error
  this

