app.createChallenge '1-3', ->

  # View class
  #
  # Instructions:
  #
  # Well look at that, we have our very first appointment.  But it isn't
  # so useful, sitting there deep down in the bowels of your browser.  To display it
  # lets first create a view class and name it AppointmentView
  #
  # initial code:
  `var AppointmentView;`
  
  # Answer:
  `AppointmentView = Backbone.View.extend({});`

